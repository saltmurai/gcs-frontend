// import { SendMissionRequest, SequenceItem } from "@/gen/mission/v1/mission_pb";
// import { create } from "zustand";

// const useStore = create((set) => ({
//   mission: new SendMissionRequest(),
//   add: (seq: SequenceItem) => {
//     set((state) => ({ mission: { ...(state.mission as SendMissionRequest) } }));
//   },
//   reset: () => {},
// }));
